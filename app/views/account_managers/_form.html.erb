<%= form_with(model: account_manager, local: true) do |form| %>
  <% if account_manager.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(account_manager.errors.count, "error") %> prohibited this account_manager from being saved:</h2>

      <ul>
        <% account_manager.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
    <p>
       <div class="page_title"> Account Manager Details 
       </div> 
    </p>
	<%= render "profiles/embedable_form", profile:account_manager.profile, allow_submit:false, form:form %>
	
	<% if false %>
	
  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :profile_id %>
    <%= form.number_field :profile_id %>
  </div>
  
	<% end %>
	
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
